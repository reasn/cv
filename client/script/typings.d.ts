interface Date {
    toLocaleDateString(locales: string[], options: any): string;
    toLocaleDateString(locales: string, options: any): string;
}

interface JQuery {
    transition(...args: any[]): JQuery;
}